-- ============================================================
--  Flyway Migration V7 – No-op
--  Các cột AI (groq_api_key, ai_free_uses_today, ai_free_uses_date)
--  đã được thêm trong session trước.
--  Migration này là placeholder để giữ version history liên tục.
-- ============================================================
SELECT 1;
